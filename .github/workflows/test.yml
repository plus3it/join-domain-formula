name: Run salt tests
on:
  pull_request:

concurrency:
  group: test-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  linux:
    uses: lorengordon/actions-workflows/.github/workflows/test-salt-linux.yml@38579474f41a00675f8ce2f0c7c79364f8e4fb12
    strategy:
      matrix:
        os_version:
          - 8
          - 9
        salt_state:
          - join-domain
        salt_pillar_root:
          - ./tests/pillar/test-linux-main
    with:
      salt-os-version: ${{ matrix.os_version }}
      salt-state: ${{ matrix.salt_state }}
      salt-pillar-root: ${{ matrix.salt_pillar_root }}

  windows:
    uses: lorengordon/actions-workflows/.github/workflows/test-salt-windows.yml@38579474f41a00675f8ce2f0c7c79364f8e4fb12
    strategy:
      matrix:
        os_version:
          - windows-2019
          - windows-2022
        salt_state:
          - join-domain
        salt_pillar_root:
          - ./tests/pillar/test-windows-main
    with:
      salt-os-version: ${{ matrix.os_version }}
      salt-state: ${{ matrix.salt_state }}
      salt-pillar-root: ${{ matrix.salt_pillar_root }}
